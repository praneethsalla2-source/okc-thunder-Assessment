{
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "cd frontend && npm install && npm run build"
    },
    "deploy": {
        "startCommand": "cd frontend && node server.js"
    }
}